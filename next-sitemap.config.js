// next-sitemap.config.js
module.exports = {
    siteUrl: 'https://www.jugnumisal.com',
    generateRobotsTxt: true,             // Optional: Automatically generates robots.txt file
    sitemapSize: 7000,                   // Optional: Adjusts the max number of URLs per sitemap file
    changefreq: 'daily',                 // Optional: Frequency of page changes
    priority: 0.7,                       // Optional: Default priority for pages
  };
  